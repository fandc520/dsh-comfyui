node_modules/
# Build output at the repo root only — an unanchored "client/" also matched
# src/client/, which quietly left the whole browser-side source untracked.
/lib/
/client/
*.tsbuildinfo
# 本地维护笔记，不对外发布/追踪
DEVELOPMENT.md
# 本机工具产物（codegraph 索引、meow-memory 记忆库），不入库
.codegraph/
.dsh-meow/
